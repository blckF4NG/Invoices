{
  "name": "invoice-generator",
  "version": "1.0.0",
  "description": "Invoice Generator Web Application",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build -w client && npm install -w server && cd server && npx prisma generate",
    "start": "cd server && npx prisma migrate deploy && node index.js"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}